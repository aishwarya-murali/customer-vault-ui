import { checkStatus } from '../utils/fetch';
import config from 'config';
import { format }      from 'url';

export function fetchIngressPortConfiguration(profile) {
  const ingressPortConfiguration = [];
  return ingressPortConfiguration;
}
