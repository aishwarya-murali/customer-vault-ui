import { checkStatus } from '../utils/fetch';
import config from 'config';
import { format }      from 'url';

export function fetchSecurityPolicies(profile, query, filter) {
  const securityPoliciesList = [];
  return securityPoliciesList;
}
